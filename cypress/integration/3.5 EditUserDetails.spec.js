import '@testing-library/cypress/add-commands';
import { findByPlaceholderText} from "@testing-library/react";

//THIS TEST NEEDS TO BE EDITED WITH THE CORRECT INPUT FIELDS INFORMATION IS ADDED SINCE THERE ARENT ANY CURRENTLY
/*
Cypress.on('uncaught:exception', (err, runnable) => {
    // returning false here prevents Cypress from
    // failing the test
    return false
  })
  
describe('Edit User Details Card', function () {
    it('should successfully Edit First Name ', function () {
       cy.findByPlaceholderText("First Name").clear()
       cy.findByPlaceholderText("First Name").type("SpongeBob");
       cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully Edit Last Name ', function () {
        cy.findByPlaceholderText("Last Name").clear()
        cy.findByPlaceholderText("Last Name").type("SquarePants");
        cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully Edit Middle Inital ', function () {
        cy.findByPlaceholderText("Middle Inital").clear()
        cy.findByPlaceholderText("Middle Inital").type("Patrick");
        cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully Edit Gender ', function () {
        cy.findByPlaceholderText("Gender").clear()
        cy.findByPlaceholderText("Gender").type("M");
        cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully Edit Date of Birth ', function () {

        cy.findByPlaceholderText("Date of Birth").clear()
        cy.findByPlaceholderText("Date of Birth").type("SquarePants");
        cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully Edit Primary Language ', function () {
        cy.findByPlaceholderText("Primary Language").clear()
        cy.findByPlaceholderText("Primary Language").type("French");
        cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully click on the Personal Details image and close edit', function () {
        cy.get('.pencil-icon').click();
            })
  })



  describe('Edit Contact Details Card', function () {
    it('should successfully Edit Home Phone Number ', function () {
       cy.findByPlaceholderText("Home").clear()
       cy.findByPlaceholderText("Home").type("816-111-2222");
       cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully Edit Work Phone Number ', function () {
        cy.findByPlaceholderText("Work").clear()
        cy.findByPlaceholderText("Work").type("816 333-2990");
        cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully click on the Personal Details image and close edit', function () {
        cy.get('.pencil-icon').click();
            })
  })



  describe('Edit Address Card', function () {
    it('should successfully Edit Physical Address ', function () {
       cy.findByPlaceholderText("Address").clear()
       cy.findByPlaceholderText("Address").type("123 game st.");
       cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully Edit Physical City ', function () {
        cy.findByPlaceholderText("City").clear()
        cy.findByPlaceholderText("City").type("Greenwood");
        cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully Edit Physical State ', function () {
        cy.findByPlaceholderText("State").clear()
        cy.findByPlaceholderText("State").type("Missouri");
        cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully Edit Physical Zip ', function () {
        cy.findByPlaceholderText("Zip").clear()
        cy.findByPlaceholderText("Zip").type("64034");
        cy.log('Hold until clear completes!!').wait(2000)
        })


    it('should successfully Edit Mailing Address ', function () {
        cy.findByPlaceholderText("Address").clear()
        cy.findByPlaceholderText("Address").type("123 game st.");
        cy.log('Hold until clear completes!!').wait(2000)
            })
    
    it('should successfully Edit Mailing City ', function () {
        cy.findByPlaceholderText("City").clear()
        cy.findByPlaceholderText("City").type("Greenwood");
        cy.log('Hold until clear completes!!').wait(2000)
        })
    
    it('should successfully Edit Mailing State ', function () {
        cy.findByPlaceholderText("State").clear()
        cy.findByPlaceholderText("State").type("Missouri");
        cy.log('Hold until clear completes!!').wait(2000)
        })
    
    it('should successfully Edit Mailing Zip ', function () {
        cy.findByPlaceholderText("Zip").clear()
        cy.findByPlaceholderText("Zip").type("64034");
        cy.log('Hold until clear completes!!').wait(2000)
        })

    it('should successfully click on the Personal Details image and close edit', function () {
        cy.get('.pencil-icon').click();
            })
  })
  */